export enum TransferType {
  RECEIVE = 'receive',
  TRANSFER = 'transfer',
  DEDUCT = 'deduct',
  RESTORE = 'restore',
}

export enum LocationType {
  SUPPLIER = 'supplier',
  WAREHOUSE = 'warehouse',
  CONTAINER = 'container',
}
