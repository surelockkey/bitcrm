{
  "name": "@bitcrm/portal-ui",
  "version": "0.0.1",
  "private": true,
  "description": "The client-facing portal UI, shared by apps/portal (the public page) and apps/web (the staff preview). Source-only: consumers transpile it.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@bitcrm/types": "*"
  },
  "peerDependencies": {
    "lucide-react": "*",
    "react": "*",
    "react-dom": "*"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "~6.9.1",
    "@testing-library/react": "~16.3.2",
    "@testing-library/user-event": "~14.6.1",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "@vitejs/plugin-react": "~6.0.3",
    "jsdom": "~29.1.1",
    "typescript": "^5",
    "vitest": "~4.1.9"
  }
}
